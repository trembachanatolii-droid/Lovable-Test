      - name: Upload dist -> Hostinger (FTPS mirror)
        env:
          HOST: ${{ secrets.HOSTINGER_HOST }}
          USER: ${{ secrets.HOSTINGER_USER }}
          PASS: ${{ secrets.HOSTINGER_PASS }}
          PORT: ${{ secrets.HOSTINGER_PORT }}
          DIR:  ${{ secrets.HOSTINGER_DIR }}
        run: |
          if [ -z "$HOST" ] || [ -z "$USER" ] || [ -z "$PASS" ] || [ -z "$DIR" ]; then
            echo "Missing one or more required secrets (HOST/USER/PASS/DIR)."
            exit 1
          fi

          lftp -e "
            set ftp:passive-mode yes;
            set ftp:ssl-allow yes;
            set ftp:ssl-force true;
            set ssl:verify-certificate no;
            set net:timeout 20;
            set net:max-retries 2;
            open -u $USER,$PASS ftp://$HOST:$PORT;
            mirror -R --delete --verbose dist/ $DIR;
            quit
          "
