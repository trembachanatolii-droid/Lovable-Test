name: Deploy to Hostinger (FTPS)

on:
  push:
    branches: ["main"]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Install lftp
        run: sudo apt-get update && sudo apt-get install -y lftp

      - name: Upload dist -> Hostinger (FTPS mirror)
        env:
          HOST: ${{ secrets.HOSTINGER_HOST }}
          USER: ${{ secrets.HOSTINGER_USER }}
          PASS: ${{ secrets.HOSTINGER_PASS }}
          PORT: ${{ secrets.HOSTINGER_PORT }}
          DIR:  ${{ secrets.HOSTINGER_DIR }}
        run: |
          if [ -z "$HOST" ] || [ -z "$USER" ] || [ -z "$PASS" ] || [ -z "$PORT" ] || [ -z "$DIR" ]; then
            echo "Missing secrets. Needed: HOSTINGER_HOST, HOSTINGER_USER, HOSTINGER_PASS, HOSTINGER_PORT, HOSTINGER_DIR"
            exit 1
          fi

          echo "Deploying dist/ to ftps://$HOST:$PORT -> $DIR"

          lftp -c "
            set ftp:passive-mode yes;
            set ftp:ssl-allow yes;
            set ftp:ssl-force true;
            set ssl:verify-certificate no;
            set net:timeout 20;
            set net:max-retries 2;
            open -u $USER,$PASS ftps://$HOST:$PORT;
            mirror -R --delete --verbose dist/ $DIR;
          "
